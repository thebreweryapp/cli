const makeAppService = require('./makeAppService');
const makeDatasource = require('./makeDatasource');
const makeDomain = require('./makeDomain');
const makeRepository = require('./makeRepository');
const makeModel = require('./makeModel');
const makeApiResource = require('./makeApiResource');

module.exports = {
  makeAppService,
  makeDatasource,
  makeDomain,
  makeRepository,
  makeModel,
  makeApiResource,
};
