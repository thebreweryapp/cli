const { firstToUpper, firstToLower } = require('./string-manipulation');
const fileWriter = require('./fileWriter');
const readFiles = require('./readFiles');

module.exports = {
  firstToLower,
  firstToUpper,
  fileWriter,
  readFiles,
};
